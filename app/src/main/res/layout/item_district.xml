<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="district"
            type="com.justnik.mosplace.domain.entities.District" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
>

            <ImageView
                android:id="@+id/iv_item_district_photo"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                tools:src="@drawable/img_district_example"
                android:scaleType="centerCrop"
                android:contentDescription="Photo Of District" />

            <TextView
                android:id="@+id/tv_item_district_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="parent"
                android:textSize="20sp"
                android:textStyle="bold"
                android:padding="4dp"
                android:text="@{district.title}"
                tools:text="Центральный административный" />
        </LinearLayout>


    </androidx.cardview.widget.CardView>
</layout>